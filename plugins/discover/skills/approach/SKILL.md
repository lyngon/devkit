---
name: approach
description: A relentless interview to sharpen a plan or design, which also writes the docs (CONCEPTS.md terms and ADRs) as we go.
disable-model-invocation: true
argument-hint: "[--no-docs]"
---

# Approach

Call the Skill tool twice, for `discover:interview` and `discover:domain-model`, then run the interview.

If the arguments contain `--no-docs`, call only `discover:interview`: sharpen the plan without writing `CONCEPTS.md` entries or ADRs.
