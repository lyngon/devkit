# Changelog

All notable changes to the `all` plugin.
Versions follow semver and are recorded in `.claude-plugin/plugin.json`.

## 0.1.0 - 2026-09-16

- Added the bundle with `repo`, `discover` and `writing`.
